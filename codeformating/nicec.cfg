class X {
public:
	char t[] = {0, 1,	2};	//
};
void calculate(int a,
	int b);
int f(int &a, int *b)
{
	a	b;
	stat1; 
	stat2;
	if (ok && i == k & 2
		|| x > 0)
	{
		for (;;)
			newresult = (b[i+1] + k) * n
				+ 4;
	}
	else
		while (ok
			&& i > 0)
			f(a, b);
	switch (i)
	{
		case 0:
			k = x ? 0 : 1;
			break;
	}
	for (i = 0; ok; i++, k++)
	{
		calculate(v1,
			v2);
	}
	do
	{
	lab:
		x = y, y = (int *)z;
	}
	while (ok);
	return (1);
}
